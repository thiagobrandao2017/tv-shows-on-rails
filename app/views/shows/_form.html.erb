<%= form_with(model: show, local: true) do |form| %>
  <% if show.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(show.errors.count, "error") %> prohibited this show from being saved:</h2>

      <ul>
      <% show.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :show_name %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :show_type %>
  </div>

  <div class="field">
    <%= form.label :language %>
    <%= form.text_field :language, id: :show_language %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :show_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
